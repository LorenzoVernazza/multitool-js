// require('./objects/hasPropTest')
// require('./objects/getPropTest')

require('./strings');

/*
function importTest(name, path) {
    describe(name, function () {
        require(path);
    });
}

describe("Multitool JS", function () {
    / beforeEach(function () {
       console.log("running something before each test");
    }); /
    importTest("objects/hasPropTest", './objects/hasPropTest');
    importTest("objects/getPropTest", './objects/getPropTest');
    importTest("strings/quote", './strings/quote');
     after(function () {
        console.log("after all tests");
    });
}); */

/* async function test() {
    await require('./objects/hasPropTest')
    await require('./objects/getPropTest')
}
test() */